# 测试报告

## 后端（pytest）

运行：

```bash
cd backend
python -m pytest --cov=app --cov-report=html:docs/htmlcov
```

结果：**16 passed**，行覆盖率 **76%**（详见 `docs/htmlcov/index.html`）。

| 测试文件 | 覆盖内容 |
| --- | --- |
| `tests/test_nlp.py`（8） | 章节切分/书名、人物抽取、关系/地点/时间线、六角色批注齐全、章末摘要、问答意图、多角色回答、辩论、脑洞、HE/BE/段落改写 |
| `tests/test_api.py`（8） | 健康检查/六角色、导入后台分析流水线、章节/批注密度过滤、ask/discuss/debate/what-if、分支创建/树/物化/三种格式导出、笔记导出、进度/书签/高亮/阅读统计、二维码、EPUB 内存构造导入、损坏 PDF 优雅降级、WebSocket 流式改写与陪读批注流 |

关键质量断言：
- 每个角色在样例书上都必须产出批注，且每条批注锚定合法段落；
- 主角唯一；地点包含强地理后缀词；
- 分支改写保留实体（主角名出现在新文本中）、段落改写只增不减；
- WebSocket 流式事件序列必须包含 `annotation` 与 `digest`。

## 前端（vitest）

运行：`cd frontend && npx vitest run`

结果：**5 passed**（字数/时长/百分比格式化、章节标签、批注按密度采样保留高优先级）。

## 手动走查清单（已执行）

- [x] 书架：封面/状态/进度/筛选/搜索/拖拽导入
- [x] 桌面三栏：目录 + 正文 + 批注/讨论/图谱/探索/分支五面板
- [x] 移动单栏：底部工具栏、底部抽屉、点按选段气泡、安全区
- [x] 10 套阅读主题、6 种字体、字号/行距/段距/字距滑块
- [x] 章末小结、金句、五角色问答/辩论/脑洞
- [x] 分支：流式生成 → 分支树 → 切换阅读 → 导出 md/txt/epub
- [x] 分享中心：金句海报/观点图/笔记长图/数据海报/原版⇄改写 GIF/二维码
- [x] PWA manifest 与离线壳、Electron 窗口加载
